{% extends "template.html.twig" %}

{% set style = '/projets/mamoun-veggie/asset/' %}

{% block stylesheets %}
    <link href="{{ style }}onerecipe.css" rel="stylesheet">
{% endblock %}

{% block content %}

 <h3 class="recipe-title">{{ recipe.title }}</h3>
<div class="last-recipe">
 
    <div class="card" style="background-image: url({{ image }}{{ recipe.image }});">
        {# <a href="{{ link }}{{ recipe.recipe_id }}"></a> #}
        </div>
        <div class="food-info">
            <div class="recipe-details">
                <span>Temps de préparation: {{ recipe.cooking_time }}</span><br>
                <span>Nombre de personnes: {{ recipe.number_of_covers }}</span>
          
           <p class="description"> {{ recipe.description }} </p>
            {{ recipe.ingredient_id }}
            {{ recipe.publication_date }}
            {{ recipe.username }}


        </div>
    </div>
    </div>
{% endblock %}